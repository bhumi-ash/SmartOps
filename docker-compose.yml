version: "3.9"

services:

  smartops:

    build: .

    container_name: smartops

    ports:
      - "5000:5000"

    env_file:
      - .env

    volumes:
      - .:/app
      - /var/run/docker.sock:/var/run/docker.sock

    command: python run.py

    restart: unless-stopped